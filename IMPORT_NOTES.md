# Import Notes

Rclone exit code: 130

Filters: source/code/docs/images under 25 MiB were copied; large binaries/media/archives/installers were left in Drive.

```
2026/05/31 03:02:26 ERROR : Using --filter is recommended instead of both --include and --exclude as the order they are parsed in is indeterminate

```
